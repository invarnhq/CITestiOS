name: CI

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:

jobs:
  build:
    if: "!startsWith(github.ref, 'refs/tags/')"
    runs-on: macos-latest
    environment: cibuild
    steps:
      - uses: actions/checkout@v4
      - uses: invarnhq/cibuild@v1
        with:
          workflow: ${{ github.event_name == 'pull_request' && 'pull-request' || 'hello-world' }}

  release:
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: macos-latest
    environment: cibuild
    steps:
      - uses: actions/checkout@v4
      - uses: invarnhq/cibuild@v1
        with:
          workflow: release
